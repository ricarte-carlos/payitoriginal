version: "3.8"

services:
  postgres:
    image: "bitnami/postgresql:16"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: docker
    ports:
      - "5432:5432"